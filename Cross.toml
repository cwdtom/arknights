[target.x86_64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture amd64",
    "apt-get update && apt-get install --assume-yes pkg-config libssl-dev:amd64",
    "apt-get update && apt-get install --assume-yes protobuf-compiler pkg-config libssl-dev:amd64"
]
